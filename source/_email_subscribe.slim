form.subscribe id="#{form_id}" action="http://smoothconversion.createsend.com/t/d/s/#{list_code}/"
  input placeholder="Your email" name="cm-#{list_code}-#{list_code}" type="email" required="required"
  input id="#{utm_id}" name="#{utm_name}" type="hidden" class="utm-input"
  button.submit =(locals[:subscribe_button] || "Submit")
